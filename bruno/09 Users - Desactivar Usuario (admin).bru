meta {
  name: Users - Desactivar Usuario (admin)
  type: http
  seq: 9
}

delete {
  url: {{host}}/users/{{userIdToDeactivate}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}
