meta {
  name: Users - Desactivar Usuario (403 token usuario)
  type: http
  seq: 10
}

delete {
  url: {{host}}/users/{{userIdToDeactivate}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}
